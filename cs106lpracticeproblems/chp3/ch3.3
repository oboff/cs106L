3. What is stream failure? How do you check for it?

Answer:
Stream failure happens when code attempts to open a non-existant file. You check a stream with the .fail() method, which returns 'true' when stream fails.
