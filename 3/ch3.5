5. Using a stringstream , write a function that converts an int into a string.

Answer:

#include <fstream>
#include <sstream>
using namespace std;

void GetInt() {
    int result;
    getline(cin, result);
    return result;
}

int main() {
    stringstream converter;
    int myInt;
    string myString;

    myInt = GetInt();

    converter << myInt;
    converter >> myString;

    return 0;
}
    
